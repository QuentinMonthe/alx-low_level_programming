#include <stdio.h>
#include "main.h"

/**
 * main - Fizz-Buzz test.
 *
 * Return: 0 to success
 */

int main(void)
{
	int i, j, n = 0, number = 612852475143;
	int tab[1000000], length = 0, div;

	for (i = 2; i < number / 2; i++)
	{
		while (j < )

	return (0);
}
