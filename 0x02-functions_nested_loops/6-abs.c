#include "main.h"

/**
 * _abs - This function compute the absolute value of an integer
 *@n: Variable to compute absolute value
 *
 * Return: A absolute value of the input
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
